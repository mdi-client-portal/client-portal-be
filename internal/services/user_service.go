package services

import "fmt"

func LoginUser(email string, password string)  {
    // 1. Cari user di DB
    fmt.Println("Email:", email)
    // 2. Verifikasi password
    // 3. Return user (atau error)
}